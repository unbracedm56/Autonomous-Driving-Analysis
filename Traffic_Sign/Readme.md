# Traffic SIgn and Signal detection model

## Takes command line input as:

### python detect.py --image {path/to/your/image.jpg}

### Script will auto download the required pre trained model and weights.
